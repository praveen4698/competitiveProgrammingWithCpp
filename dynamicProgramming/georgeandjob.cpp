#include<bits/stdc++.h>
using namespace std;
#define PI 3.14159265
static const int INF = 0x3f3f3f3f;
static const long long INFL = 0x3f3f3f3f3f3f3f3fLL;

    const int lim = 5001;
    long int dp[lim][lim];
    long int A[lim];
    long int solve(int i,int k)
    {
        if(dp[i][k] != -INF)
            return dp[i][k];
        if( k == 1)
        {
            dp[i][k] =
        }
    }
    int main()
    {


        return 0;
    }
